# API routes (if you have any) - exclude from fallback
/api/*  /api/:splat  200

# Static assets - serve directly
/assets/*  /assets/:splat  200
/icons/*   /icons/:splat   200

# SPA routing fallback - all routes should serve index.html
/*    /index.html   200
